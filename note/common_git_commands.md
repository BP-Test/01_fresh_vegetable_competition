# Common Git Commands

## Dealing with remote branch

1. Delete remote branch
```bash

git push <origin_name> -d <branch_name>

```
2. Show remote branch 

```bash

git branch -r
git fetch -p
```

3. Update remote branch status

```bash

git fetch -p
```
